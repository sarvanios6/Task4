.csv FILE DATA content read with using json format.

* convert the data .csv to Json after that using Json Parser to read the content in this program.
* set the path of file reader from .json format file in the parser. when read the Json data will be stored to JsonArray.
* JsonArray data's filter to print the first and last name list in console for who is consume the current product. here using the if (condition) get and filter the current product name for the Json file. 